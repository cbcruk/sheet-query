{
  "name": "@sheet-query/examples",
  "private": true,
  "description": "Runnable examples exercising the core against real Google Sheets.",
  "type": "module",
  "scripts": {
    "dev": "vp dev browser",
    "build": "vp build browser",
    "build:pages": "vp build browser --base=/sheet-query/",
    "preview": "vp preview browser",
    "verify-read": "node verify-read.ts",
    "discoveries": "node discoveries.ts"
  },
  "dependencies": {
    "sheet-query": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vite-plus": "catalog:"
  }
}
